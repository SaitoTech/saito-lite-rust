import { Keystore } from '../types';
export declare class KeystoreAuth {
    keystore?: Keystore;
    constructor(keystore?: Keystore);
    signToken(signatrue: string, requestID: string): string;
}
