import type { StorageEntry } from '../../../primitive/types';
import type { Registry } from '../../../types';
interface ManualMetadata {
    docs: string;
    type: string;
}
/** @internal */
export declare function createRuntimeFunction(prefix: string, method: string, key: Uint8Array | string, { docs, type }: ManualMetadata): (registry: Registry) => StorageEntry;
export {};
